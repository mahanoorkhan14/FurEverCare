# Requested Revision

- Removed the technology list from the footer.
- Replaced runtime remote photo URLs with local image files in `assets/images/`.
- Replaced visible emoji UI glyphs with relevant locally bundled Bootstrap Icons.
- Added strict complete-email validation to Veterinarian Setup and Feedback forms (for example, `name@example.com` is accepted while `name@domain` is rejected).
- Rebuilt the website demo video to demonstrate the updated portal.
